class User{
  String email, password, name, phone, credit, quantity, verify, datereg, address;

  User({
    this.email,
    this.password,
    this.name,
    this.phone,
    this.credit,
    this.quantity,
    this.verify,
    this.datereg,
    this.address
  });
}