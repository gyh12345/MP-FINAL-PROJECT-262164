class Product{
  String id, name, size, quantity, price, date, type, address, deliprice;

  Product({
    this.id,
    this.name,
    this.size,
    this.quantity,
    this.price,
    this.date,
    this.type,
    this.address,
    this.deliprice
  });
}